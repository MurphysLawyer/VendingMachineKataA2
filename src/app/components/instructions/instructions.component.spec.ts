/* tslint:disable:no-unused-variable */

import { InstructionsComponent } from './instructions.component';

describe('InstructionsComponent', () => {
  it('should create', () => {
    let component = new InstructionsComponent();
    expect(component).toBeTruthy();
  });
});
